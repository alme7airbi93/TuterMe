<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
        xmlns="http://www.springframework.org/schema/security"
        xmlns:beans="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security-4.1.xsd">

    <global-method-security secured-annotations="enabled" />

    <http auto-config="true">
        <intercept-url pattern="/**" access="permitAll" />
        <intercept-url pattern='/admin/**' access='hasRole("ADMIN")' />
        <intercept-url pattern='/instructor/**' access='hasRole("ADMIN,MODERATOR,INSTRUCTOR")'/>
        <intercept-url pattern='/moderator' access='hasRole("MODERATOR")' />
        <!-- access denied page -->
        <!-- this is my first time on spring security -->
        <form-login login-processing-url="/login"
                    login-page="/login"
                    username-parameter="username"
                    password-parameter="password"
                    default-target-url="/home"
                    authentication-failure-url="/login?auth=fail" />

        <logout logout-url="/logout" logout-success-url="/logout" />

        <csrf disabled="true"/>
    </http>

    <authentication-manager>
        <authentication-provider>
            <!--<password-encoder hash="md5" />-->
            <!-- if not working add datasource up there -->
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query="select USERNAME, PASSWORD, ENABLED from USER where username = ?"
                               authorities-by-username-query="select USERNAME, ROLE from AUTHORIZATION where username =? " />
        </authentication-provider>
    </authentication-manager>

</beans:beans>